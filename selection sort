def sorting():
    array= list(map(int,input().split()))
    size=len(array)
    for i in range(0,size):
        for j in range(i+1,size):
            if array[i]>array[j]:
                temp=array[i]
                array[i]=array[j]
                array[j]=temp
    print(array)
sorting()
